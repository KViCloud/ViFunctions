{{- if .Values.goBuilder.service.type }}
1. Access the Go Builder service:
   http://{{ .Release.Name }}-go.{{ .Values.namespace }}.svc.cluster.local:{{ .Values.goBuilder.service.port }}

{{- end }}
{{- if .Values.pythonBuilder.service.type }}
2. Access the Python Builder service:
   http://{{ .Release.Name }}-python.{{ .Values.namespace }}.svc.cluster.local:{{ .Values.pythonBuilder.service.port }}

{{- end }}

3. Use the following command to get the logs of the Go Builder:
   kubectl logs -l app={{ include "images-builder.fullname" . }}-go -n {{ .Values.namespace }}

4. To uninstall the chart:
   helm uninstall {{ .Release.Name }} --namespace {{ .Values.namespace }}